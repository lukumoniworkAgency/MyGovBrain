import type { Metadata } from "next";
import Link from "next/link";
import { PageHeader } from "@/components/page-header";
import { SiteShell } from "@/components/site-shell";
import { getPageLanguage } from "@/lib/language";
export const metadata: Metadata = { title: "Job Hub — GovGuide", description: "Government jobs, apprenticeships, internships, and private opportunities." };
const jobs = [["Government Job", "Junior Assistant — State Administration", "Assam", "Graduate", "Apply soon"], ["Private Job", "Customer Support Executive", "Remote", "Any graduate", "Immediate"], ["Apprenticeship", "Digital Skills Apprentice", "Assam", "Class 10+", "Apply soon"], ["Internship", "Civic Tech Product Intern", "Remote", "Undergraduate", "Rolling"]] as const;
export default async function JobsPage({ searchParams }: { searchParams: Promise<{ lang?: string }> }) { const { languages, languageCode } = await getPageLanguage((await searchParams).lang); return <SiteShell languages={languages} languageCode={languageCode}><main id="main-content"><PageHeader kicker="Opportunities" title="Find work that moves your future forward" lead="Search government roles, private openings, apprenticeships, and internships in one place." /><section className="mx-auto max-w-6xl px-5 py-10 sm:px-8 sm:py-14"><div className="grid gap-4 sm:grid-cols-3">{[["Open roles","24"],["Government","12"],["Remote","8"]].map(([label,value]) => <div key={label} className="rounded-xl border border-slate-200 bg-white p-5"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-3xl font-bold">{value}</p></div>)}</div><div className="mt-10 flex flex-wrap items-center justify-between gap-4"><div><h2 className="text-2xl font-semibold">Latest opportunities</h2><p className="mt-1 text-sm text-slate-600">Example listings shown for the current product preview.</p></div><Link href="/dashboard" className="inline-flex min-h-11 items-center rounded-md border border-teal-700 px-4 py-2 text-sm font-semibold text-teal-800">View saved jobs</Link></div><div className="mt-5 grid gap-4 md:grid-cols-2">{jobs.map(([type,title,location,eligibility,status]) => <article key={title} className="rounded-xl border border-slate-200 bg-white p-5 transition hover:border-teal-400 hover:shadow-sm"><div className="flex items-center justify-between gap-3"><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-semibold text-blue-700">{type}</span><span className="text-xs font-medium text-teal-700">{status}</span></div><h3 className="mt-4 text-lg font-semibold">{title}</h3><div className="mt-3 flex flex-wrap gap-4 text-sm text-slate-600"><span>{location}</span><span>{eligibility}</span></div><div className="mt-5 flex gap-3"><Link href="/ai" className="inline-flex min-h-10 items-center rounded-md bg-teal-700 px-4 py-2 text-sm font-semibold text-white">Check eligibility</Link><Link href="/auth?next=/jobs" className="inline-flex min-h-10 items-center rounded-md border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700">Save job</Link></div></article>)}</div></section></main></SiteShell>; }
