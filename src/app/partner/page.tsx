import type { Metadata } from "next";
import Link from "next/link";
import { PageHeader } from "@/components/page-header";
import { SiteShell } from "@/components/site-shell";
import { getPageLanguage } from "@/lib/language";
export const metadata: Metadata = { title: "CSC Partner Portal — GovGuide", description: "Manage customer requests, services, and center performance." };
export default async function PartnerPage({ searchParams }: { searchParams: Promise<{ lang?: string }> }) { const {languages,languageCode}=await getPageLanguage((await searchParams).lang); return <SiteShell languages={languages} languageCode={languageCode}><main id="main-content"><PageHeader kicker="CSC partner portal" title="Grow your center with GovGuide" lead="A clear workspace for citizen requests, service processing, and center performance." /><section className="mx-auto max-w-6xl px-5 py-10 sm:px-8 sm:py-14"><div className="grid gap-4 sm:grid-cols-3">{[["Open requests","12"],["This month","186"],["Partner rating","4.8"]].map(([label,value]) => <div key={label} className="rounded-xl border border-slate-200 bg-white p-5"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-3xl font-bold">{value}</p></div>)}</div><div className="mt-8 grid gap-5 lg:grid-cols-2"><div className="rounded-xl border border-slate-200 bg-white p-6"><h2 className="text-xl font-semibold">Customer requests</h2><p className="mt-3 text-sm text-slate-600">Requests, processing status, and document checklists will be managed here.</p><Link href="/auth?next=/partner" className="mt-5 inline-flex rounded-md bg-teal-700 px-4 py-2 text-sm font-semibold text-white">Sign in to partner portal</Link></div><div className="rounded-xl border border-slate-200 bg-white p-6"><h2 className="text-xl font-semibold">Service catalog</h2><p className="mt-3 text-sm text-slate-600">Keep your offered services accurate so citizens can find the right help.</p><Link href="/register-agent" className="mt-5 inline-block text-sm font-semibold text-teal-800 underline underline-offset-4">Manage center registration</Link></div></div></section></main></SiteShell>; }
