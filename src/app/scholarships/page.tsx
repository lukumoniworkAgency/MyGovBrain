import type { Metadata } from "next";
import Link from "next/link";
import { PageHeader } from "@/components/page-header";
import { SiteShell } from "@/components/site-shell";
import { getPageLanguage } from "@/lib/language";
export const metadata: Metadata = { title: "Scholarship Hub — GovGuide", description: "Find scholarships, check eligibility, and track deadlines." };
const scholarships = [["Merit Scholarship", "Students", "Class 10–12", "31 Aug 2026"], ["Girls STEM Scholarship", "Women students", "Undergraduate", "15 Sep 2026"], ["Rural Startup Grant", "Entrepreneurs", "Any", "30 Sep 2026"]] as const;
export default async function ScholarshipsPage({ searchParams }: { searchParams: Promise<{ lang?: string }> }) { const { languages, languageCode } = await getPageLanguage((await searchParams).lang); return <SiteShell languages={languages} languageCode={languageCode}><main id="main-content"><PageHeader kicker="Funding your future" title="Scholarships made easier to find" lead="Explore scholarships by category, education level, state, and income criteria. Save a listing to receive deadline reminders." /><section className="mx-auto max-w-6xl px-5 py-10 sm:px-8 sm:py-14"><div className="grid gap-4 md:grid-cols-3">{scholarships.map(([title,category,level,deadline]) => <article key={title} className="rounded-xl border border-slate-200 bg-white p-5"><span className="rounded-full bg-teal-50 px-3 py-1 text-xs font-semibold text-teal-800">{category}</span><h2 className="mt-4 text-lg font-semibold">{title}</h2><p className="mt-2 text-sm text-slate-600">{level}</p><p className="mt-4 text-sm font-semibold text-blue-700">Deadline: {deadline}</p><div className="mt-5 flex gap-3"><Link href="/ai" className="text-sm font-semibold text-teal-800 underline underline-offset-4">Check eligibility</Link><Link href="/auth?next=/scholarships" className="text-sm font-semibold text-slate-700 underline underline-offset-4">Save</Link></div></article>)}</div></section></main></SiteShell>; }
