import type { Metadata } from "next";
import Link from "next/link";
import { PageHeader } from "@/components/page-header";
import { SiteShell } from "@/components/site-shell";
import { getPageLanguage } from "@/lib/language";
export const metadata: Metadata = { title: "Your Dashboard — GovGuide", description: "Your citizen control center." };
export default async function DashboardPage({ searchParams }: { searchParams: Promise<{ lang?: string }> }) { const {languages,languageCode}=await getPageLanguage((await searchParams).lang); return <SiteShell languages={languages} languageCode={languageCode}><main id="main-content"><PageHeader kicker="Citizen control center" title="Welcome back" lead="Track applications, keep documents ready, and pick up where you left off." /><section className="mx-auto max-w-6xl px-5 py-10 sm:px-8 sm:py-14"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["Recent applications","3"],["Saved services","5"],["Saved CSC centers","2"],["Notifications","7"]].map(([label,value]) => <div key={label} className="rounded-xl border border-slate-200 bg-white p-5"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-3xl font-bold">{value}</p></div>)}</div><div className="mt-8 grid gap-5 lg:grid-cols-2"><div className="rounded-xl border border-slate-200 bg-white p-6"><h2 className="text-xl font-semibold">Quick actions</h2><div className="mt-4 grid gap-3 sm:grid-cols-2">{[["Apply for a service","/services"],["Upload document","/vault"],["Find a CSC","/csc"],["Track application","/tracker"]].map(([label,href]) => <Link key={href} href={href} className="rounded-lg border border-slate-200 px-4 py-3 text-sm font-semibold text-slate-800 hover:border-teal-400">{label}</Link>)}</div></div><div className="rounded-xl border border-slate-200 bg-white p-6"><h2 className="text-xl font-semibold">Recent activity</h2><p className="mt-4 text-sm text-slate-600">Your latest application updates and alerts will appear here.</p><Link href="/notifications" className="mt-5 inline-block text-sm font-semibold text-teal-800 underline underline-offset-4">View notifications</Link></div></div></section></main></SiteShell>; }
