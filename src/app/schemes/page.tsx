import type { Metadata } from "next";
import Link from "next/link";
import { PageHeader } from "@/components/page-header";
import { SiteShell } from "@/components/site-shell";
import { getPageLanguage } from "@/lib/language";
export const metadata: Metadata = { title: "Government Schemes — GovGuide", description: "Discover schemes for students, farmers, women, senior citizens, and entrepreneurs." };
const schemes = [["Student Support Scheme", "Students", "Tuition support and learning resources."], ["Farmer Modernization Scheme", "Farmers", "Access to equipment and market support."], ["Women Entrepreneurship Scheme", "Women", "Support for small enterprises and self-employment."], ["Senior Citizen Welfare Scheme", "Senior citizens", "Assistance, healthcare, and pension support."]] as const;
export default async function SchemesPage({ searchParams }: { searchParams: Promise<{ lang?: string }> }) { const { languages, languageCode } = await getPageLanguage((await searchParams).lang); return <SiteShell languages={languages} languageCode={languageCode}><main id="main-content"><PageHeader kicker="Public benefits" title="Find the right government scheme" lead="Search benefits by audience, understand eligibility and documents, and get clear application guidance." /><section className="mx-auto max-w-6xl px-5 py-10 sm:px-8 sm:py-14"><div className="grid gap-4 md:grid-cols-2">{schemes.map(([title,category,description]) => <article key={title} className="rounded-xl border border-slate-200 bg-white p-5"><p className="text-xs font-bold uppercase tracking-wider text-teal-700">{category}</p><h2 className="mt-3 text-xl font-semibold">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-600">{description}</p><div className="mt-5 flex gap-4 text-sm font-semibold"><Link href="/ai" className="text-teal-800 underline underline-offset-4">Check eligibility</Link><Link href="/contact" className="text-slate-700 underline underline-offset-4">Get help</Link></div></article>)}</div></section></main></SiteShell>; }
